# FitnessApp Desktop Launch

Use the desktop shortcut named FitnessApp to launch the app.

The shortcut runs:
- scripts/launch-fitnessapp.ps1

This script starts the backend and frontend locally so the app is available at:
- http://localhost:3000
